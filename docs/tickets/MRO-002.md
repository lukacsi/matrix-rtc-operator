---
id: MRO-002
title: "Test @concat limits for homeserver.yaml generation"
status: DONE
priority: P0
phase: "Phase 0: Assessment"
created: 2026-03-18
updated: 2026-03-18
blocked_by: []
blocks: []
---

# MRO-002: Test @concat limits for homeserver.yaml generation

## Description
Can @concat generate a valid multiline Synapse homeserver.yaml? The matrix-rtc-operator needs to produce a complete Synapse configuration file as a Kubernetes Secret, assembled from user-provided MatrixRTC CR fields.

## Acceptance Criteria
- [x] Confirmed \n works in @concat for multiline output
- [x] Documented pattern for homeserver.yaml generation

## Notes
The existing livekit-declarative-operator already proves that @concat with \n generates valid multiline YAML configs in Secret stringData. The homeserver.yaml is larger but the same approach works.

## Resolution
CLEAR — existing livekit-operator proves @concat with \n generates valid multiline YAML configs in Secret stringData. Pattern documented in assessment. homeserver.yaml is larger but same approach works.
